<style is:inline>
  :root {
    --color-background: #1f1f1f;
    --color-text: #f1f1f1;
    --color-input-background: #3b3b3b;
    --color-input-border-focus: #96e9fd;
    --color-input-text: #fff;
    --color-button-background: #004b74;
    --color-button-background-hover: #069;
    --color-button-text: #fff;
    --color-link: var(--color-text);
    --color-link-hover: #96e9fd;
    --size-base: 8px; /* reference to calc spacing and others values */
  }

  /* Josh's Custom CSS Reset: https://www.joshwcomeau.com/css/custom-css-reset/ */

  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  * {
    margin: 0;
  }

  html,
  body {
    height: 100%;
  }

  img,
  picture,
  video,
  canvas,
  svg {
    display: block;
    max-width: 100%;
  }

  input,
  button,
  textarea,
  select {
    font: inherit;
  }

  p,
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    overflow-wrap: break-word;
  }

  /* Theming styles */

  body {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background-color: var(--color-background);
    color: var(--color-text);
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
      Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
      sans-serif;
    font-size: 1.125rem;

    /* CSS reset */
    -webkit-font-smoothing: antialiased;
    line-height: 1.5;
  }

  main {
    display: flex;
    flex-direction: column;
    max-width: 500px;
    padding: var(--size-base);
    gap: calc(var(--size-base) * 5);
  }

  a {
    color: var(--color-link);
  }

  a:hover,
  a:focus {
    color: var(--color-link-hover);
  }

  input {
    padding: var(--size-base);
    border: none;
    border-radius: var(--size-base);
    outline: none;
    background-color: var(--color-input-background);
    color: var(--color-input-text);
  }

  input:focus {
    box-shadow: 0 0 0 2px var(--color-input-border-focus);
  }

  button {
    padding: var(--size-base);
    border: none;
    border-radius: var(--size-base);
    background-color: var(--color-button-background);
    color: var(--color-button-text);
    font-weight: bold;
  }

  button:hover,
  button:focus {
    background-color: var(--color-button-background-hover);
  }
</style>
